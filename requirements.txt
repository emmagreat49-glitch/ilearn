# iLEARN — Python dependencies
# Install with: pip install -r requirements.txt

# ── Web Framework (Python) ──────────────────────────
flask>=2.3.0
gunicorn

# ── Email & Environment Management ──────────────────
python-dotenv>=1.0.0
resend>=2.0.0

# ── Data Science & Analytics (Python) ──────────────
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
numpy>=1.24.0

# ── Note on Java component ──────────────────────────
# The GradeAnalyzer (java/GradeAnalyzer.java) requires Java 11+.
# Install Java: https://adoptium.net/
# Compile: javac java/GradeAnalyzer.java -d java/out
# The Flask app auto-compiles and runs it via subprocess.
# If Java is not installed, the app uses a Python fallback automatically.